namespace apis

import com.amazon.ask.types.builtins.AMAZON.DATE
import com.amazon.ask.types.builtins.AMAZON.DURATION
import com.amazon.ask.types.builtins.AMAZON.TIME
import com.amazon.ask.types.builtins.AMAZON.DayOfWeek
import slotTypes.service
import types.VolunteerRequest
import types.ValidatedArgsOnce
import types.ValidatedArgsRecurring

// api call to request a volunteer
action VolunteerRequest APIRequestVolunteer()

// api call to validate arguments for a single volunteer request
// saves parameters in session
action ValidatedArgsOnce APIValidateArgsOnce(DURATION duration, TIME time, DATE date, service service)

// api call to validate arguments for a recurring volunteer request
// saves parameters in session
action ValidatedArgsRecurring APIValidateArgsRecurring(service service, DayOfWeek dow, TIME time, DURATION duration, DATE since, DATE until)